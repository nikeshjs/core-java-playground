package OOP;

public class Mobile {
    int ram;
    String brand;
    int storage;
    String processor;

    public void Output(){
        System.out.println("New phone");
    }
}

/* In Java, classes and main are kept in separate files (recommended).
If in same package, no need to do anything. Otherwise, specify the package or simply import.
Everything should be inside the class.
Nothing exists outside the class. Everything of Java should be inside the class.
 */
